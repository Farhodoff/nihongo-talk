import React from 'react';

/**
 * PWA Install Prompt - Disabled per user preference to prevent annoying banners on mobile.
 */
export const InstallPrompt: React.FC = () => {
  return null;
};

export default InstallPrompt;
